{
  "name": "browserify-playground",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "$(npm bin)/browserify index.js -o bundle.js -t [ babelify --presets [ es2015 ] ]",
    "watch": "$(npm bin)/watchify index.js -o bundle.js -t [ babelify --presets [ es2015 ] ]"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.14.0",
    "babelify": "^7.3.0",
    "browserify": "^13.1.0",
    "watchify": "^3.7.0"
  }
}
